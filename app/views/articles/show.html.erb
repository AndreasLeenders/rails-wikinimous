

<h3><%= @article.title %></h3>
<p><%= @article.content %></p>

<br>

<%= link_to "Back to index", articles_path %>

<h2><%=link_to  "Edit articles", edit_article_path(@article)%></h2>


<h2><%=link_to  "delete", article_path(@article), method: :delete,
      data: { confirm: "Are you sure?" }%></h2>
